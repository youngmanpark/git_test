def add(alpha: int, beta: int) -> int:
    return alpha + beta


def subtract(alpha: int, beta: int) -> int:
    return alpha - beta


def multiply(alpha: int, beta: int) -> int:
    return alpha * beta


def divide(alpha: int, beta: int) -> int:
    return alpha // beta, alpha % beta
